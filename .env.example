# Session (required for auth)
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# App URL (for payment callbacks and redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SSLCommerz (optional; if not set, payment init returns sandbox redirect to success)
SSLCOMMERZ_STORE_ID=
SSLCOMMERZ_STORE_PASSWD=
SSLCOMMERZ_IS_LIVE=false

# bKash (optional; if not set, payment init returns sandbox redirect to success)
BKASH_APP_KEY=
BKASH_APP_SECRET=
BKASH_IS_SANDBOX=true
